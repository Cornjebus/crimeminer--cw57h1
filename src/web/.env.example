# CrimeMiner Web Application Environment Configuration
# Version: 1.0.0
# Security: FedRAMP High & CJIS Compliant

# API Gateway Configuration
# Required: HTTPS endpoint with TLS 1.3+ and valid certificate
# Format: https://<domain>/<version>
VITE_API_URL=https://api.crimeminer.gov/v1

# WebSocket Configuration
# Required: WSS endpoint with TLS 1.3+ for real-time notifications
# Format: wss://<domain>
VITE_WS_URL=wss://ws.crimeminer.gov

# Application Configuration
# Required: Display name for UI elements
# Validation: Max 50 chars, alphanumeric with spaces
VITE_APP_NAME=CrimeMiner

# Required: Semantic versioning for tracking
# Format: MAJOR.MINOR.PATCH
VITE_APP_VERSION=1.0.0

# UI Theme Configuration
# Optional: WCAG 2.1 AA compliant theme setting
# Allowed values: light, dark
VITE_DEFAULT_THEME=light

# API Request Configuration
# Optional: Timeout in milliseconds (5000-60000)
# Default: 30 seconds
VITE_API_TIMEOUT=30000

# Logging Configuration
# Optional: Controls log verbosity and sensitive data
# Allowed values: debug, info, warn, error
# Note: debug level restricted in production
VITE_LOG_LEVEL=info